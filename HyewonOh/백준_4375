one="1"

try:
    while(True):
        A=int(input())

        answer=one
        while(True):

            if int(answer)%A==0:
                print(len(answer))
                break

            answer+='1'
except: pass

#EOF 입력받을 때는 try except 문을 사용한다
